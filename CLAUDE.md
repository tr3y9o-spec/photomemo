# 写真メモ｜Claude Code 運用規約

このファイルは毎セッション読まれる。短く保つ。

**実装・変更の前に `写真メモシステム_仕様書.md` を読むこと。**

---

## 0. これは何か

自分で撮った写真やスクショに、**メモ・日付・URL・タグ**を付けて、フォルダへ納めるためのアプリ。

`Projects\sandbox` で試作し、第1版が通ったのでここへ独立した（サンドボックス仕様書 §5・型B）。

**ここは蓄積する側。** 置いたものは残る前提で扱う。試作のときの「いずれ全部消える」は、もう当てはまらない。

---

## 1. このシステムで最も重要なこと

**「保存」は常に1タップで、常に成功する。**

これを守るために、3つを固定してある。**崩す変更は、別のアプリを作っていることになる。**

| 決めごと | 実装 |
|---|---|
| **必須項目を作らない** | フォルダは開いた時点で選択済み。メモ・タグ・URL は任意。日付は既定値が入る |
| **保存後に聞き返さない** | 保存＝モーダルが閉じて保存先が開く。確認ダイアログを出さない |
| **あとから直せる** | タイルをタップすれば同じモーダルが編集モードで開く。だから保存時に何も書かなくてよい |

通信を保存の前に置かない。同期は必ず背景で行う。**保存が通信を待った時点でこの約束は死ぬ。**

削除は別扱いでよい（判断が要るので確認を出す）。

---

## 2. 3つのベース

root 直下のこの3点は**消さない・作り替えない**。

| ファイル | 役割 |
|---|---|
| `起動.bat` | 入口。`cd /d "%~dp0"` → `claude` の3行のみ。書き換えない |
| `CLAUDE.md` | このファイル。毎セッション読まれる運用規約 |
| `写真メモシステム_仕様書.md` | 実装指示書。構成・設計の決めごとはここ |

---

## 3. 権威順位 — 何を根拠に状態を述べるか

1. **動いているアプリの実体**（`app\` の作業ツリー）と **E2E の結果**
2. `写真メモシステム_仕様書.md`
3. 過去の log・会話

上位と下位が食い違ったら上位が正しい。

**「動くはず」で状態を述べない。** E2E を回してから言う。

---

## 4. フォルダの使い分け

| 場所 | 何を置くか |
|---|---|
| `app\` | アプリ本体（PWA）。ブラウザで動くものだけ |
| `app\test\` | E2E。ブラウザを実際に立ち上げて確かめる |
| `gas\` | Google Apps Script 側の受け口。ここだけ動く場所が違う |
| `.github\workflows\` | 配信（GitHub Pages） |

構成の詳細は仕様書 §3。

---

## 5. 守るもの

ここは**実データを扱う側**になった。守る対象が試作のときと入れ替わっている。

| 置かない | 理由 |
|---|---|
| **GAS の合言葉（TOKEN）** | 秘密情報。端末の localStorage にだけ置く。コードにも git にも書かない |
| **個人の写真そのもの** | 写真は端末と本人の Drive の中にある。リポジトリには入れない |
| 個人情報を含むテスト用データ | テストは `ダミー画像を5枚入れる` で足りる |

リポジトリを public にする場合、**入っているのはコードだけ**である状態を保つ。

---

## 6. 作業の進め方

- 変更前に何をするかを説明する
- 削除・上書きは実行前に確認を取る
- **変更したら E2E を回す。** 通らないまま「済んだ」と書かない
- 置いただけのものを「済んだ」と書かない
- §1 の3つに触る変更は、先に「なぜ崩すのか」を言葉にする
