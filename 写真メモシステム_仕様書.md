# 写真メモシステム 仕様書

> 自分で撮った写真・スクショに、メモを添えて仕舞っておくための道具
> 作成: 2026-09-16

---

## 0. このドキュメントの使い方

Claude Code に渡す実装指示書。フォルダ直下に置き、`CLAUDE.md` から参照する。

**実装前に必ず読むこと。特に §4（背骨）。** ここを崩す変更は、別のアプリを作っていることになる。

---

## 1. 目的

写真やスクショに、あとから意味が分かる情報を添えて仕舞う。

既存のメモアプリは「挿入 → フォトから → 画像選択」と工数がかかり、保存先も保存を押したあとに聞かれる。
そこを **画像を選ぶ → 行き先が最初から並んでいる → 1タップで終わり** にする。

---

## 2. どこから来たか

`Projects\sandbox`（試作システム）で試作し、その仕様書 §5 の手順で**型B＝新規環境として独立**した。
サンドボックスの git 履歴は引き継いでいない。

独立時点で確かめてあったこと:

- 保存が1タップで完結し、聞き返さない（確認ダイアログ0）
- フォルダが既定で選択済み。未選択で開かない
- 複数枚を1回のモーダルで捌ける（フォルダ・タグは一括、メモ・日付・URL は1枚ごと）
- zip で出して戻せる（`メタ.json` は人が読める）
- 共有シート / `?url=` で URL を受けられる
- メタだけの GAS 同期が往復する（全消し → 引き直し → 写真を選び直してメモが戻る）

E2E 91件・JS エラー0 で通った状態を、そのまま持って出ている。

試作時に見送った判断:

- Instagram の保存済みを読む公式手段が無いため、第1版では扱わない（§10）
- 画像そのものの同期は入れない。取り返しがつかないのはメタのほうだから（§8）

---

## 3. フォルダ構成

```
Projects\photomemo\
├── 起動.bat                     Claude Code 起動用（ベース1）
├── CLAUDE.md                    Claude Code への指示（ベース2）
├── 写真メモシステム_仕様書.md     このファイル（ベース3）
├── .gitignore
├── app\                         アプリ本体（PWA）
│   ├── index.html               画面の骨
│   ├── style.css                見た目
│   ├── app.js                   流れ・モーダル・整理・書き出し読み込み・同期の配線
│   ├── db.js                    IndexedDB（サムネと原寸を分ける／削除の墓標）
│   ├── sync.js                  同期キュー（メタだけ・背景）
│   ├── lib.js                   EXIF / サムネ / ハッシュ / zip（外部ライブラリなし）
│   ├── manifest.webmanifest     PWA の宣言。share_target もここ
│   ├── sw.js                    オフライン用
│   ├── icon.svg                 アイコン
│   └── test\e2e.js              E2E（Chromium を実際に立ち上げる）
├── gas\
│   └── コード.gs                 Google Apps Script 側の受け口
└── .github\workflows\pages.yml  app\ を GitHub Pages へ
```

**`input\` `data\` `config\` `logs\` は作っていない。** このアプリに要らないため。
（サンドボックス仕様書 §2 の呼び方から意図的に外している。中身がデータ処理ではなく Web アプリなので）

**外部ライブラリを使っていない。** ブラウザ標準だけで書いてある。足すときは、それ自体を1つの判断として扱う。

---

## 4. 背骨 — 崩してはいけない3つ

**「保存」は常に1タップで、常に成功する。**

| 決めごと | 実装上どうなっているか |
|---|---|
| 必須項目を作らない | `openNew()` で必ずフォルダを1つ選択済みにする（今いるフォルダ → 前回使ったフォルダ → 先頭）。他は全部任意 |
| 保存後に聞き返さない | `save()` は確認を挟まない。保存先フォルダを開き、取り消しはトースト1本だけ |
| あとから直せる | タイルをタップ → `openEdit()` で同じモーダル。保存時に何も書かなくてよい根拠がこれ |

**通信を保存の前に置かない。** 保存はローカル（IndexedDB）へ即確定し、同期は 1.5 秒後に背景で走る。
圏外でも保存でき、同期が失敗しても保存は無かったことにならない。

削除は別扱い。判断が要るので確認を出し、トーストから取り消せるようにしてある。

---

## 5. 画面と流れ

```
フォルダ一覧 ──タップ──▶ 画像グリッド ──タップ──▶ モーダル（表示・編集）
     ▲                        │
     └────── ‹ 戻る ──────────┘

＋写真から / ◉カメラ ──▶ モーダル（保存）──保存──▶ 保存先フォルダのグリッド
```

モーダルに並ぶもの（上から）: プレビュー ／ **保存先フォルダ** ／ **タグ** ／ メモ ／ 日付 ／ URL ／ 保存。

- 390×844（iPhone 相当）で、フォルダ・タグ・メモ・保存はスクロールなしで見える
- 日付と URL は1スクロール下。どちらも既定値が入るので、届かなくても1タップ保存は成立する
- プレビューはタップで拡大・縮小（スクショの文字を読むため）

### 複数枚を選んだとき

モーダルは1回だけ。`全部に` と `この1枚に` のラベルで適用範囲を分けている。

| 全部に | この1枚に |
|---|---|
| 保存先フォルダ・タグ | メモ・日付・URL |

---

## 6. フォルダとタグ

役割を分けてある。**ここを曖昧にすると、保存のたびに迷うことになる。**

| | フォルダ | タグ |
|---|---|---|
| 個数 | 必ず1つ（排他） | 0個以上 |
| 数 | 少数・固定 | 増える |
| 見た目 | 角丸の四角 | 丸 |
| 1枚を複数の場所に | **できない** | できる（こちらで解決する） |

タグはモーダルに常時12個まで。並び順は **使用回数 → 直近**。それ以上は「すべて」で開く。
改名・統合・削除はメニューから（既にある名前に書き換えると統合）。

タグは名前そのものを各レコードに持たせている。だから改名すると全件書き換えが走るが、
`メタ.json` とスプレッドシートが人に読める形になる。**この取引は意図的。**

---

## 7. 日付の決まり方

`撮影日(EXIF DateTimeOriginal)` → 無ければ `ファイルの日時` → 無ければ `今日`。

どれを使ったかはラベル横に出る。手で直せる。
スクショや Instagram 由来の画像は EXIF を持たないので、自然にファイル日時へ落ちる。

---

## 8. 同期（メタだけ・GAS）

**取り返しがつかないのは画像ではなくメモのほう。**
画像はカメラロールに元があるが、メモ・日付・URL・タグはどこにもない。しかも1件あたり数百バイトしかない。

だから**メタだけ**を Google スプレッドシートへ送る。画像は送らない。

```
保存を押す ─▶ ローカルに即確定 ─▶ モーダルが閉じる   ← 通信を待つ場所が無い
                  └─ 1.5秒後、背景で push → pull
```

### 置き方

`gas\コード.gs` の冒頭に手順がある。要点:

1. スプレッドシートを作り、拡張機能 → Apps Script に `gas\コード.gs` を貼る
2. スクリプト プロパティに `TOKEN` を作る（長いランダム文字列）。**コードには書かない**
3. `準備()` を1回実行
4. ウェブアプリとしてデプロイ（実行: 自分 ／ アクセス: **全員**）
5. `/exec` の URL を、アプリのメニュー →「同期の設定」に貼る

「全員」でないとブラウザから呼べない。だから合言葉で守る。合言葉は端末の localStorage にだけ入る。

### 消えたあと、どう戻るか

1. 全部消えた状態で「今すぐ同期」→ **メモが「画像待ち」のタイルとして戻る**
2. 同じ写真を選び直すと、**ハッシュが一致したメモが自動でその画像に付く**（増えたりしない）

削除は墓標（`deletedAt` の行）として伝わるので、消したものは復活しない。

### 踏んだ罠

| 罠 | 対処 |
|---|---|
| GAS は OPTIONS に応えないので、`application/json` だと CORS preflight で必ず落ちる | `Content-Type: text/plain` で送り、単純リクエストにする。**E2E で「OPTIONS が0回」を検証している** |
| フォルダを改名しても向こうに伝わらない | 行の `folder` 列はフォルダ名。改名時に中の画像の `updatedAt` を上げて送り直す |
| 実行時間6分/回・コールドスタート数秒 | 行だけなので当たらない。背景同期なので誰も待たない |

**競合は「更新時刻が新しいほうが勝つ」だけ。** 2台で同じ画像を別々に書き換えると片方が黙って負ける。

---

## 9. 持ち出し（zip）

メニュー → `書き出す（zip）` / `読み込む（zip）`

```
photomemo_YYYY-MM-DD.zip
├── メタ.json          フォルダ・タグ・全項目（メモ／日付／URL／ハッシュ）
└── images/<id>.<ext>  原寸画像
```

- 無圧縮 zip。`unzip` でも Python の `zipfile` でも普通に開ける
- `メタ.json` は人が読める。別の形へ移すときはここから変換できる
- 読み込み時、フォルダは**名前で**引き当てる（同名があれば合流、無ければ作る）
- サムネイルは zip に入れず、読み込み時に作り直す

---

## 10. Instagram について

**保存済み（コレクション）を読む公式の手段は無い。**
Instagram Graph API に該当の口が無く、旧 Basic Display API も終了済み。**ここへ行かない。**

代わりに **URL を受ける口だけ**ある。

- `manifest.webmanifest` の `share_target`（Android のインストール済み PWA で共有シートに出る）
- `?url=...` / `?text=...` で開いた場合も拾う
- 拾った URL は画面上部で預かり、次に保存する画像の URL 欄へ入る
- モーダルの `貼付` でクリップボードからも入れられる

画像そのものは付いてこない。要るならスクショを撮って取り込む。
他人の投稿画像を複製して溜める作りにしていないのは、著作権と利用規約の両方で危ういため。

---

## 11. 動かし方

`file://` では動かない。IndexedDB・`crypto.subtle`・Service Worker が
**安全なコンテキスト**（`https` か `localhost`）を要るため。

```
npx -y serve app        # or: npx -y http-server app
# → http://localhost:3000 を開く
```

同じ Wi-Fi のスマホから `http://<PCのIP>:3000` でも開けるが、それは安全なコンテキストではない。
**Service Worker・ホーム画面への追加・`share_target` は効かず、`crypto.subtle` も使えない。**
ハッシュが簡易版に落ちるので、`https` に移したときに §8 の「選び直すとメモが戻る」が食い違う。
**試す配信方法は途中で変えない。**

メニュー → `ダミー画像を5枚入れる` で、実データ無しに一通り試せる。

### テスト

```
npm i -g playwright
CHROME_PATH=<chromium の実行ファイル> node app/test/e2e.js
```

91件。**変更したら必ず回す。** 通らないまま「済んだ」と書かない。

同期のテストは、偽の GAS を**別ポート（＝別オリジン）**に立て、**OPTIONS に応えない**ようにしてある。
`application/json` で送るよう変えれば、その時点で落ちる。

---

## 12. 扱わないもの

意識して外してある。思い付きで足さない。足すときは、それ自体を1つの判断として扱う。

| | 理由 |
|---|---|
| Instagram 保存済みの読み取り | 公式の手段が無い（§10） |
| 画像そのものの同期 | メタだけで、取り返しがつかない部分はほぼ守れる。画像は重く quota も踏む |
| 複数端末での同時編集 | 競合は「新しいほうが勝つ」だけ。突き詰めていない |
| 画像の加工・書き込み | 元画像を壊さない方針。メモは別レコードで持つ |
| 動画 | 範囲外 |
| スクショの文字検索（OCR） | 効きそうだが、まず本体を確かめる |
| 外部ライブラリ | 標準だけで足りている |

---

## 13. 分かっている弱いところ

- **画像はまだブラウザの中だけ。** iOS の PWA はストレージを回収されうる。メタは GAS へ逃がしたが、画像は端末のカメラロール頼み
- **本物の GAS に対する CORS の挙動は未検証。** E2E は別オリジン・OPTIONS 非対応の偽サーバで再現しているだけ。実デプロイで一度確かめること
- **HEIC**（iPhone 標準）はブラウザによっては開けない。その場合サムネイルは作られずタイルにファイル名が出る。保存はできる
- カメラは OS のカメラを1枚ずつ開く方式（`capture` 属性）。連写はできない
- 重複は目印を出すだけで、保存は止めない

---

## 14. 壊してはいけないもの

- **§4 の背骨3つ** — 崩す変更は、先に「なぜ崩すのか」を言葉にしてから
- **root 直下の3つのベース** — 変えるときは、それ自体を1つの判断として扱う
- **`app\test\e2e.js`** — 通らなくなったら、テストではなくコードを直す。**テストを緩めて通さない**
- **合言葉をコードに書かない** — 端末の localStorage にだけ置く（`CLAUDE.md` §5）
